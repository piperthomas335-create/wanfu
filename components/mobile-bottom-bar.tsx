import { Phone, MessageCircle, MapPin } from 'lucide-react'
import { shop } from '@/lib/site-data'
export function MobileBottomBar(){const items=[[shop.telHref,'電話予約',Phone],[shop.lineUrl,'LINE',MessageCircle],[shop.mapUrl,'地図・ナビ',MapPin]] as const;return <div className="fixed inset-x-0 bottom-0 z-50 grid grid-cols-3 border-t border-paper/20 bg-ink text-paper lg:hidden">{items.map(([href,label,Icon])=><a key={label} href={href} target={href.startsWith('http')?'_blank':undefined} rel={href.startsWith('http')?'noopener noreferrer':undefined} className="flex min-h-16 flex-col items-center justify-center gap-1 border-r border-paper/15 text-[10px] tracking-wider"><Icon className="size-4"/>{label}</a>)}</div>}
